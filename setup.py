from distutils.core import setup

setup(
        name='TameofThrones',
        version='1.0',
        packages=['tameofthrones', ],
        license='Apache 2.0',
        # long_description=open('README.rst').read(),
)

